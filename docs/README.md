# Docs

Two documents live here:

| File | Purpose |
|---|---|
| `ARCHITECTURE.md` | Current technical architecture — read this first |
| `Colchis Creamery - ბრენდბუქი.md` | Brand book (design system, typography, brand voice) |

For **commands**, **migration workflow**, **legacy-to-avoid**, and **known follow-ups**, see [`/CLAUDE.md`](../CLAUDE.md) at the repo root — that file is the authoritative current reference and is auto-loaded into every Claude Code session.

## Removed in the cleanup pass

The four previous handoff docs (`PHASE_7a_HANDOFF.md`, `PHASE_7b_8_HANDOFF.md`, `PHASE_9_HANDOFF.md`, `implementation plan.md`) were point-in-time session-bootstrap notes from earlier development phases. Their content is now incorporated into `CLAUDE.md` + the per-phase commit messages in `git log` (Phase 1-8 of the major overhaul, May 2026). They were removed to prevent confusion from stale data — for instance, the old "Phase 9" referred to a different phasing scheme than the Phase 1-8 architecture overhaul.

Git history preserves them at any pre-cleanup commit.
